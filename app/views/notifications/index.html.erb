<section class="hero">
  <div class="hero-body">
    <div class="container">
      <h1 class="title">
        通知一覧
      </h1>
    </div>
  </div>
</section>

<article class="media">
  <article class="media">
    <% if @notifications.exists? %>
      <div class="users-index">
        <%= render @notifications %>
      </div>
    <% else %>
      <p>通知はありません</p>
    <% end %>
  </article>
</article>

